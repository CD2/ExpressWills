%section.top-bar-section
  %ul.left
    %li= link_to("Express Will", root_url)
    %li= link_to("Benefits of a will", benefits_of_a_will_path)
  %ul.right
    %li= link_to "Contact Us", contact_path
    - if signed_in?
      - if current_user.admin?
        %li= link_to 'All Wills', admin_wills_path
        %li= link_to "Log Out", signout_path, method: "delete"
      - else
        %li= link_to "New Will", new_will_path

        %li= link_to "Wills", wills_path
        %li= link_to "Log Out", signout_path, method: "delete"
      
    - else
      %li= link_to "Sign In", signin_path
      %li= link_to "Register", new_user_path