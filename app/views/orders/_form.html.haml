= form_for @order do |f|

  .field
    = f.label :full_name
    = f.text_field :full_name
  .field
    = f.label :email_address
    = f.text_field :email_address
  .field
    = f.label :will_id
    = f.number_field :will_id
  .actions
    = f.submit 'Save'
